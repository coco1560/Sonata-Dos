; ============================================================================
; REBOOT.MVT — 重启 DOS(内核释放本程序并重入内核 main)
; ============================================================================
main:
  jmp reboot_entry
